#!/usr/bin/node
//  Base class to declare a Rectangle with its dimensions.

class Rectangle {
  constructor (w, h) {
    if (w > 0 && h > 0) {
      this.width = w;
      this.height = h;
    }
  }
}
module.exports = Rectangle;
